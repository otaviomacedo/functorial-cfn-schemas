{
  "name": "functorial-cfn-compiler",
  "version": "0.1.0",
  "description": "Compiler from abstract templates (functorial schemas) to CloudFormation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "fibers": "tsc && node dist/compiler/src/fiber-cli.js",
    "viz": "tsc && node dist/compiler/src/viz-server.js"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.16.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
